import React from 'react'

const Count = ({count}) => {
    return (
        <div>
                <div className="count">
      <h4>{count}</h4>
    </div>
        </div>
    )
}

export default Count
